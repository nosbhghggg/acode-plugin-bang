# Bang 插件使用说明

> 适用插件版本：v0.4.0+　适用编辑器：Acode（安卓，建议最新版）

---

## 一、安装（一次性）

1. 把 `plugin.zip` 传到手机（微信文件传输助手 / 数据线 / 网盘均可）
2. 打开 Acode → 左侧边栏的**插件图标**（或 设置 → 插件）
3. 右上角 `+` → 选 **Local（本地安装）** → 选中 `plugin.zip`
4. 安装完确认插件已**启用**
5. 建议重启一次 Acode

装好后打开任意 `.mdtlbl` 文件，顶部会出现 `Bang 插件已加载` 提示，代码有彩色高亮——说明生效了。

---

## 二、核心概念

| 文件类型 | 是什么 | 插件对它的能力 |
|---|---|---|
| `.mdtlbl` | Bang 源码（你写的代码） | 高亮、补全、编译成 mlog、保存自动查错 |
| `.mlog` / `.logic` | Mindustry 逻辑指令（游戏能跑的） | 高亮、反编译回 Bang、格式化 |

**关键流程**：手机写 `.mdtlbl` → 编译成 `.mlog` → 复制剪贴板 → 游戏里粘贴导入。

---

## 三、日常使用（最简单路径）

### 写代码 → 进游戏

1. Acode 里新建文件 `test.mdtlbl`，写 Bang 代码：
   ```
   i = 0; do {
       x, y = cos(i)*r, sin(i)*r;
   } while ++i < 360;
   ```
2. 点编辑器右侧的 **「Bang」悬浮按钮** → 选「**复制 mlog 到剪贴板**」
   （或者命令面板执行 `Bang: 编译并复制 mlog 到剪贴板`）
3. 切到 Mindustry → 点逻辑处理器 → 编辑 → **从剪贴板导入**
4. 完成，逻辑就跑起来了

### 保存自动编译

保存 `.mdtlbl` 时插件会自动编译：
- 成功 → 同目录自动生成/更新同名 `.logic` 文件 + 提示"编译通过（N 行）"
- 失败 → 提示"Bang 编译错误：xxx"（帮你当场发现问题）

---

## 四、Bang 悬浮按钮菜单

打开 `.mdtlbl` 或 `.mlog` 文件时，编辑器右侧出现 **Bang** 按钮，点它出菜单。

**在 `.mdtlbl`（源码）文件里：**

| 菜单项 | 作用 |
|---|---|
| 编译并保存 .logic | 编译结果写到同目录 `.logic` 文件 |
| 编译并预览 | 编译结果开个新窗口看，不落盘 |
| 查看标签码（t） | 输出带标签的中间形态，便于排查流程 |
| 复制 mlog 到剪贴板 | 最常用：复制后去游戏里导入 |
| 从剪贴板导入 Bang 源码 | 从复制的 mlog 里还原出源码 |

**在 `.mlog` / `.logic`（游戏指令）文件里：**

| 菜单项 | 作用 |
|---|---|
| 反编译为 Bang 源码 | 把 mlog 变回 Bang，方便改老代码 |
| 格式化 mlog（原地） | 整理缩进与标签，原地替换 |
| 导入嵌入的 Bang 源码 | 若 mlog 里带源码（见下），直接还原 |
| 从剪贴板导入 Bang 源码 | 同上，从剪贴板读 |

---

## 五、源码嵌入（重要特性）

插件默认把 Bang **源码**一起打包写进编译产物的末尾（`print ">DATA:…"` 那几行）。
好处：**游戏里把 mlog 复制出来，粘回 Acode 就能还原完整源码**——手机上也能像桌面一样保源码往返。

- 手机上与我做的桌面 VSCode 插件**互相兼容**，哪边嵌的源码都能在另一边还原
- 想关掉：命令面板执行 `Bang: 切换「mlog 嵌入源码」开关`
- 注意：源码体积大的时候会自动跳过嵌入（保持 mlog 大小可用）

---

## 六、命令面板（不点按钮也能用）

在 Acode 命令面板搜索 `Bang`，可用的命令：

- `Bang: 编译当前文件（mlog 输出到新文件）`
- `Bang: 编译并复制 mlog 到剪贴板`
- `Bang: 反编译当前 mlog 为 Bang 源码`
- `Bang: lint 检查当前文件`
- `Bang: 导入 mlog 中嵌入的源码`
- `Bang: 从剪贴板导入 Bang 源码`
- `Bang: 切换「mlog 嵌入源码」开关`
- **`Bang: 自检（诊断插件状态）`** ← 出问题时先跑这个

---

## 七、补全与高亮

- 输入时自动弹补全：mlog 指令、`do/while/if/const` 等关键字、`@unit/@thisx` 等内置变量
- 键入 `@` 会专门提示内置变量
- 输入 `For!`、`CountLoop!`、`do-while` 等会提示常用片段

**若补全不自动弹出**：补全走的是 Acode 的全局补全机制，需要在
**设置 → 编辑器 → 实时自动补全（liveAutoCompletion）** 打开；
另外请勿关闭「语言补全（languageCompletion）」，否则会连插件补全一起屏蔽。

---

## 八、常见问题

**Q：打开文件没有高亮、也没有 Bang 按钮？**
A：① 确认插件已启用；② 卸载旧版重装新版（旧版有高亮 bug）；③ 执行 `Bang: 自检`，把弹窗内容发出来。

**Q：没有 Bang 悬浮按钮？**
A：该按钮需要 Acode v316+（最新正式版都有）。若确认是最新版仍没有，多半是插件没加载——跑自检命令。

**Q：编译报"无法加载 WASM"？**
A：卸载重装，确保 `plugin.zip` 里的两个 `.wasm` 文件都装进去了（自检命令会显示编译器状态）。

**Q：游戏里导入后不运行？**
A：检查编译器是否报了错（编译成功才会有"N 条指令"提示）；游戏内导入要用处理器编辑界面的 **Edit → Import from Clipboard**。

**Q：想要"一键直接注入游戏"，不复制粘贴？**
A：需要额外装 Mindustry 的 **MlogWatcher** 模组（支持 v159），插件后续版本会支持直连；当前先走剪贴板。

---

## 九、当前版本边界

- 编译完全离线，不联网、不需要电脑
- 暂不支持直接注入游戏（需等后续版本 + MlogWatcher 模组）
- mlog 行内波浪线诊断尚未实现（现在是保存后在提示里报首行错误）
